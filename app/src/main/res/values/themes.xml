<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Musichino" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black_MixMe</item>
        <item name="colorPrimaryVariant">@color/purple_dark_gradient</item>
        <item name="colorOnPrimary">@color/purple_mid_gradient</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/purple_light_gradient</item>
        <item name="colorOnSecondary">@color/purple_mid_gradient</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>


        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Musichino.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:alertDialogStyle">@style/AlertDialogCustom</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="Theme.Musichino.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:background">@color/transparent</item>
    </style>

    <style name="Theme.Musichino.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>